package example

object Work {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(56); 

	var s = Lists.sum(null);System.out.println("""s  : Int = """ + $show(s ))}
	
}
